import { Container, Stack, Text } from "native-base";
import { SafeAreaView } from "react-native";

export default function Maps() {
  return (
    <SafeAreaView>
      <Stack>
        <Text fontSize="xs">Ubicación</Text>
        <Container>

        </Container>
      </Stack>
    </SafeAreaView>
  )
}